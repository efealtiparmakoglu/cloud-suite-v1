apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cloud-suite-v1-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: cloud-suite-v1.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: cloud-suite-v1-service
            port:
              number: 80
